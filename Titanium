package default

import scala.concurrent.duration._

import io.gatling.core.Predef._
import io.gatling.http.Predef._
import io.gatling.jdbc.Predef._

class Titanium extends Simulation {

        val web = http
                .baseUrl("https://aptitanium.com")
                .acceptHeader("text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8")
                .acceptLanguageHeader("en-US,en;q=0.5")
                .acceptEncodingHeader("gzip, deflate")
                .userAgentHeader("load-test")

        val landing = scenario("Landing").exec(http("/5faffec4415f478cb169bf1cdd44ca5c").get("/5faffec4415f478cb169bf1cdd44ca5c"))

        setUp(landing.inject(
                rampUsersPerSec(0) to(2) during(10 seconds),
                constantUsersPerSec(2) during(120 minutes) randomized
        ))
        .protocols(web);
}
